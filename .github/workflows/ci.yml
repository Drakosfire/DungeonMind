name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Sync dependencies (locked)
        run: uv sync --locked

      - name: Lint
        run: uv run ruff check .

      - name: Core importable without heavy extras
        run: |
          uv run --no-dev python -c "import dungeonmind"

      - name: Unit tests
        run: uv run pytest -m "not integration"

  # PostgreSQL integration job activates in PR B (migrations + pgvector
  # substrate). Kept as a commented scaffold so the wiring is reviewable here.
  # integration:
  #   runs-on: ubuntu-latest
  #   services:
  #     postgres:
  #       image: pgvector/pgvector:pg16
  #       env:
  #         POSTGRES_DB: dungeonmind
  #         POSTGRES_USER: dungeonmind
  #         POSTGRES_PASSWORD: dungeonmind-ci
  #       ports: ["5432:5432"]
  #       options: >-
  #         --health-cmd "pg_isready -U dungeonmind -d dungeonmind"
  #         --health-interval 5s --health-timeout 5s --health-retries 10
  #   env:
  #     DUNGEONMIND_PG_DSN: postgresql://dungeonmind:dungeonmind-ci@localhost:5432/dungeonmind
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: astral-sh/setup-uv@v5
  #     - run: uv sync --locked --extra postgres
  #     - run: uv run pytest -m integration
